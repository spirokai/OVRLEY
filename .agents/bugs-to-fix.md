-left_right_balance widget: Both preview and render have spaces around the '/' separating left and right values; these spaces must be removed from both frotend and backend.
-Scale gradient triangle angle by +50% in preview and in render; otherwise the ascents/descents look too shallow.
-Add: Two marker types for elevation/route widgets - A) one with thin concentric circle; same color as the main circle, add slider for diameter B) one with a larger solid circle underneath the main circle, 50% opacity, same color as the main circle, slider for diameter. Add a dropdown in the marker section of route/elevation that toggles between these two types and the current single circle marker. The semi-transparent circle must be rendered below the main circle in both preview and render; ensure correct laying in Skia.
-In preview only, the marker and the label get squished in the vertical direction when the entire widget is scaled down below certain treshhold. It only happens at small heights. The render is correct - mirror the backend approach in the frontend to prevent this squishing from happening in the preview as well.
-Add CTRL+C / CTRL+V keyboard shortcuts for copying/pasting widget settings in the editor. When copying, serialize the settings of the currently selected widget to a JSON string and store it in the clipboard. When pasting, if the clipboard contains a valid JSON string that matches the widget settings schema, apply those settings to the currently selected widget. This allows users to easily duplicate widgets with the same configuration or transfer settings between different widgets.
