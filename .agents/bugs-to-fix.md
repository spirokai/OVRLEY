-Add: Two marker types for elevation/route widgets - A) one with thin concentric circle; same color as the main circle, add slider for diameter B) one with a larger solid circle underneath the main circle, 50% opacity, same color as the main circle, slider for diameter. The semi-transparent circle must be rendered below the main circle in both preview and render; ensure correct laying in Skia. Add a dropdown in the marker section of route/elevation that toggles between these two types and the current single circle marker - is it realistic to show the markers in the dropdown as well, or is that too complicated?
-In preview only, the marker and the label of elevation profile get squished in the vertical direction when the entire widget is scaled vertically down below certain treshhold - even if the height is still sufficient to display all. It only happens at small heights. The render is correct - mirror the backend approach in the frontend to prevent this squishing from happening in the preview as well.
